package com.ackdev.statics.forms;

public enum FormPermissionEnum {
    //	READONLY, CREATEFORM, FORMENTRY, DELETEFORM, DELETEDATAENTRY
    ADMINISTRATOR, SUBMITTER, REPORTER, SUPERVISOR
//	Administrator (All), Submitter (Entry only), Reporter (Read only), Supervisor (Read & Entry)
}
